function tmp() {
    return true;
}

describe('tmp', () => {
    test('should return true', () => {
        const result = tmp();
        expect(result).toBe(true);
    });
});

//anything for testing update
